<div id="editDataModal" class="fixed inset-0 z-50 flex items-center justify-center hidden bg-black bg-opacity-50">
    <div class="w-full max-w-2xl transition-all transform bg-white rounded-lg shadow-xl">
        <div class="flex items-center justify-between px-6 py-4 border-b">
            <h3 class="text-xl font-semibold text-gray-800">Edit Data Penelitian PNBP</h3>
            <button onclick="closeModal('editDataModal')" class="text-gray-500 hover:text-gray-800"><span
                    class="material-icons-outlined">close</span></button>
        </div>

        <form id="editDataForm" method="POST" _method="PUT">
            <div class="p-6 max-h-[60vh] overflow-y-auto space-y-4">
                <% formFields.forEach(field => { %>
                    <div>
                        <label for="edit_<%= field.name %>" class="block text-sm font-medium text-left text-gray-700">
                            <%= field.label %>
                            <% if (field.required) { %>
                                <span class="text-red-500">*</span>
                            <% } %>
                        </label>
                        <% if (field.type === 'select') { %>
                            <select 
                                name="<%= field.name %>" 
                                id="edit_<%= field.name %>"
                                class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-[#4F46E5] focus:border-[#4F46E5]"
                                <% if (field.required) { %>required<% } %>>
                                <option value="" selected disabled>Pilih <%= field.label %></option>
                                <% field.options.forEach(option => { %>
                                    <option value="<%= option %>" <%= (field.value === option) ? 'selected' : '' %>>
                                        <%= option %>
                                    </option>
                                <% }); %>
                            </select>
                        <%# Kondisi untuk tipe input 'anggota' (dynamic member input) untuk edit %>
                        <% } else if (field.type === 'anggota') { %>
                            <div id="editAnggotaContainer">
                                <!-- Dynamic anggota inputs will be populated by JavaScript -->
                            </div>
                            <button 
                                type="button" 
                                onclick="addEditAnggotaInput()"
                                class="mt-2 flex items-center px-3 py-2 text-sm text-[#4F46E5] border border-[#4F46E5] rounded-md hover:bg-[#4F46E5] hover:text-white transition-colors">
                                <span class="mr-1 text-sm material-icons-outlined">add</span>
                                Tambah Anggota
                            </button>
                        <% } else { %>
                            <input 
                                type="<%= field.type %>" 
                                name="<%= field.name %>" 
                                id="edit_<%= field.name %>"
                                value="<%= field.value || '' %>"
                                class="mt-1 block w-full border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-[#4F46E5] focus:border-[#4F46E5]"
                                <% if (field.required) { %>required<% } %>>
                        <% } %>
                        <p class="hidden mt-1 text-xs text-red-500" id="error-<%= field.name %>"><%= field.label %> wajib diisi.</p>
                        <% if (field.type === 'number') { %>
                            <p class="hidden mt-1 text-xs text-red-500" id="type-error-<%= field.name %>"><%= field.label %> harus berupa angka.</p>
                        <% } %>
                    </div>
                <% }); %>
            </div>
            <div class="px-6 py-4 space-x-3 text-right bg-gray-50">
                <button onclick="closeModal('editDataModal')" type="button"
                    class="px-4 py-2 text-sm font-medium text-gray-800 bg-gray-200 border border-gray-300 rounded-md shadow-sm hover:bg-gray-300 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-400">
                    Batal
                </button>
                <button type="submit"
                    class="inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-[#4F46E5] hover:bg-[#4338CA] focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-[#4338CA]">
                    Simpan Perubahan
                </button>
            </div>
        </form>
    </div>
</div>

<script>
function addEditAnggotaInput() {
    const container = document.getElementById('editAnggotaContainer');
    const newInputGroup = document.createElement('div');
    newInputGroup.className = 'edit-anggota-input-group flex items-center space-x-2 mb-2';
    
    newInputGroup.innerHTML = `
        <input 
            type="text" 
            name="Anggota[]" 
            placeholder="Nama Anggota"
            class="flex-1 border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-[#4F46E5] focus:border-[#4F46E5]">
        <button 
            type="button" 
            onclick="removeEditAnggotaInput(this)"
            class="px-3 py-2 text-red-600 border border-red-300 rounded-md hover:bg-red-50 focus:outline-none">
            <span class="text-sm material-icons-outlined">remove</span>
        </button>
    `;
    
    container.appendChild(newInputGroup);
    updateEditRemoveButtons();
}

function removeEditAnggotaInput(button) {
    const inputGroup = button.parentElement;
    inputGroup.remove();
    updateEditRemoveButtons();
}

function updateEditRemoveButtons() {
    const inputGroups = document.querySelectorAll('.edit-anggota-input-group');
    inputGroups.forEach((group, index) => {
        const removeButton = group.querySelector('button');
        if (inputGroups.length > 1) {
            removeButton.style.display = 'block';
        } else {
            removeButton.style.display = 'none';
        }
    });
}

function populateEditAnggotaFields(anggotaArray) {
    const container = document.getElementById('editAnggotaContainer');
    container.innerHTML = ''; // Clear existing inputs
    
    // If no anggota data, create one empty input
    if (!anggotaArray || anggotaArray.length === 0) {
        anggotaArray = [''];
    }
    
    anggotaArray.forEach((anggota, index) => {
        const inputGroup = document.createElement('div');
        inputGroup.className = 'edit-anggota-input-group flex items-center space-x-2 mb-2';
        
        inputGroup.innerHTML = `
            <input 
                type="text" 
                name="Anggota[]" 
                placeholder="Nama Anggota"
                value="${anggota || ''}"
                class="flex-1 border border-gray-300 rounded-md shadow-sm py-2 px-3 focus:outline-none focus:ring-[#4F46E5] focus:border-[#4F46E5]">
            <button 
                type="button" 
                onclick="removeEditAnggotaInput(this)"
                class="px-3 py-2 text-red-600 border border-red-300 rounded-md hover:bg-red-50 focus:outline-none"
                style="display: none;">
                <span class="text-sm material-icons-outlined">remove</span>
            </button>
        `;
        
        container.appendChild(inputGroup);
    });
    
    updateEditRemoveButtons();
}

// Function to be called when edit modal opens
function initializeEditAnggotaInputs() {
    updateEditRemoveButtons();
}
</script>